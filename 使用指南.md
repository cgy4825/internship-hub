# 使用指南（每次打开网站怎么操作）

> 这个网站是**本地运行**的，用两个双击脚本就可以，不用敲命令。
> 脚本在 `D:\ideaProject\internship-hub\` 里。

---

## 🚀 日常打开网站（每次）

**双击 `打开网站.bat`**（英文界面，但别管它，等它自己完成）

- 它会自动：① 启动本地网站服务器 ➜ ② 准备好后**自动打开浏览器**访问 `http://localhost:5300`
- 看到浏览器弹出网站页面，就成功了。
- 那个黑色窗口（cmd）**不要关**，关了就停了。
- 用完想停，把黑色窗口关掉即可。

> 如果第一步等了很久浏览器没弹出来，就手动在浏览器打开 `http://localhost:5300`。

---

## 📥 更新数据（让岗位变新）

**双击 `更新数据.bat`**

- 它会自动：① 抓取最新校招/实习数据 ➜ ② 提交 ➜ ③ 推送到 GitHub
- 完成后，**刷新网站页面**就能看到最新岗位。
- 建议：**每天或隔几天点一次**就行，不用每次都点。

> 想更省事？GitHub 上已经配了每天自动采集（不过云端抓国内网站不一定稳，所以手动点一下最保险）。

---

## ⚠️ 两个脚本各是什么

| 脚本 | 作用 | 多久用一次 |
|------|------|-----------|
| `打开网站.bat` | 启动网站 + 自动开浏览器 | 每次想用时 |
| `更新数据.bat` | 抓最新岗位 + 推 GitHub | 每天或隔几天 |

---

## ❗ 常见问题

**Q: 双击报错 / 黑屏一闪而过？**
重新双击一次。首次运行会 `npm install` 装依赖，需要等一会儿（别关）。

**Q: 浏览器没自动打开？**
手动打开 `http://localhost:5300`。

**Q: 点「打开网站」但一直加载不出来？**
先双击 `更新数据.bat` 更新数据，再刷新页面。若还不行，看黑色窗口里有没有红色报错。

**Q: 我想在手机上看？**
确保电脑和手机连同一 WiFi，手机浏览器访问 `http://电脑IP:5300`（电脑IP可在 `ipconfig` 里找）。目前只支持电脑上用最省事。

---

## 一句话总结

> **想用 → 双击「打开网站.bat」；想让岗位变新 → 双击「更新数据.bat」。**
